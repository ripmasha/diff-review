# Bearer token required on all /v1 routes.
SERVICE_TOKEN=replace-me

# Google AI Studio API key, used by the "llm" review provider.
GEMINI_API_KEY=replace-me

# Gemini model used by the "llm" review provider.
GEMINI_MODEL=gemini-3.6-flash
